


declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'admino';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'coorde';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'entre';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'lead';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'lect';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'oper';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'super';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'supere';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'coord';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO


declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'adminp';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
Go

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'ing';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'ingdeproc';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO

declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'techadmin';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'View SAP Notifications'
GO



declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'oper';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'Process SAP Notifications'
GO


declare @RoleId bigint;
select @RoleId = r.Id from Role r where SiteId = 9 and Alias = 'super';
insert into RoleElementTemplate (RoleElementId, RoleId)
select re.Id, @RoleId
from RoleElement re
where re.Name = 'Process SAP Notifications'
GO



GO

