
--- these are likely already set correctly for Lubes but I'm doing this update anyway to make sure it's set this way when we release
update SiteConfigurationDefaults set CopyTargetAlertResponseToLog = 1 where SiteId = 10; -- lubes
update WorkAssignment set CopyTargetAlertResponseToLog = 1 where SiteId = 10; -- lubes






GO



INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Administrator' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Administrator' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Administrator' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Administrator' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'Create Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'Edit Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'Delete Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Area Team Lead' and re.[Name] = 'Comment on Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Approve Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Reject Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Create Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Edit Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Delete Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Comment on Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Toggle Approval Required for Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Respond to Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Engineering' and re.[Name] = 'Configure Pre-Approved Target Ranges';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Lead Technician' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Lead Technician' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Lead Technician' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Lead Technician' and re.[Name] = 'Respond to Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Approve Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Reject Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Create Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Edit Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Delete Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Comment on Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Toggle Approval Required for Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Respond to Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operations Coordinator' and re.[Name] = 'Configure Pre-Approved Target Ranges';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operator' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operator' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operator' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operator' and re.[Name] = 'Respond to Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Read User' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Read User' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Read User' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Read User' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'View Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'View Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'View Navigation - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'View Priorities - Targets';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Approve Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Reject Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Create Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Edit Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Delete Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Comment on Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Toggle Approval Required for Target Definition';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Respond to Target Alerts';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Supervisor' and re.[Name] = 'Configure Pre-Approved Target Ranges';



GO


alter table SiteConfiguration add DefaultTargetDefinitionRequiresResponseWhenAlertedValue bit null;
go

update SiteConfiguration set DefaultTargetDefinitionRequiresResponseWhenAlertedValue = 0;
go

alter table SiteConfiguration alter column DefaultTargetDefinitionRequiresResponseWhenAlertedValue bit not null;
go

-- lubes
update SiteConfiguration set DefaultTargetDefinitionRequiresResponseWhenAlertedValue = 1 where SiteId = 10;




GO

