
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Operator' and re.[Name] = 'Print Permit';

INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'Delete Permit';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'Create Permit Request';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'Edit Permit Request';
INSERT INTO RoleElementTemplate (RoleElementId, RoleId) select re.Id, r.Id from RoleElement re, Role r where r.SiteId = 10 and r.[Name] = 'Scheduler' and re.[Name] = 'Delete Permit Request';




GO

