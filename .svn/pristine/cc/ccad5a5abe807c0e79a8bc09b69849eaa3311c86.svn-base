namespace Com.Suncor.Olt.Client.Validation
{
    /// <summary>
    /// Validates the work permit view for "basic" things defined in the view.
    /// Non-basic things like what data is required in which sections are handled elsewhere
    /// (see IWorkPermitSectionsValidator).
    /// </summary>
    public interface IWorkPermitViewValidator
    {
        bool ValidateHasError();
    }
}