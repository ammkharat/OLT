

insert into RoleElementTemplate (RoleElementId, RoleId) 
select re.Id, r.Id
from Role r, RoleElement re
where r.SiteId = 10 and r.Name = 'Administrator' and
      re.Name in ('Configure Work Permit Contractor', 'Configure Craft Or Trade', 'Configure Configured Document Links')




GO

